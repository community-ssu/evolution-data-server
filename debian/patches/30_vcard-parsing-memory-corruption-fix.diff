Index: evolution-data-server.build/addressbook/libebook-dbus/e-vcard.c
===================================================================
--- evolution-data-server.build.orig/addressbook/libebook-dbus/e-vcard.c	2008-08-11 15:29:23.000000000 +0100
+++ evolution-data-server.build/addressbook/libebook-dbus/e-vcard.c	2008-08-11 15:30:16.000000000 +0100
@@ -501,7 +501,7 @@
 			}
 			if (str->len != 0) {
 				attr_group = g_string_free (str, FALSE);
-				g_string_assign (str, "");
+				str = g_string_new ("");
 			}
 		}
 		else if (g_unichar_isalnum ((uc = g_utf8_get_char (lp))) ||c == '-' || c == '_') {
